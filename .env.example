# Supabase Postgres connection (async driver -> use postgresql+asyncpg://)
# Get this from Supabase project settings -> Database -> Connection string
DATABASE_URL=postgresql+asyncpg://postgres:password@db.xxxxxxxx.supabase.co:5432/postgres

# Used later for Supabase Auth JWT verification (Phase 2) — included here
# only as a placeholder so .env.example stays a single reference file.
SUPABASE_JWT_SECRET=
SUPABASE_URL=
SUPABASE_ANON_KEY=
